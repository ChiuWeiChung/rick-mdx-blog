export enum NoteQuerySort {
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt',
  // TITLE = 'title',
  // CATEGORY = 'category',
  // TAGS = 'tags',
  // VISIBLE = 'visible',
}

export enum QueryOrder {
  ASC = 'asc',
  DESC = 'desc',
}
