export enum NoteQuerySort {
  CREATED_AT = 'created_at',
  UPDATED_AT = 'updated_at',
  TITLE = 'title',
  CATEGORY = 'category',
  TAGS = 'tags',
  VISIBLE = 'visible',
}

export enum QueryOrder {
  ASC = 'asc',
  DESC = 'desc',
}
